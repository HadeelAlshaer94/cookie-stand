
// object each object has key value
let test = { 
name : 'seattle',
// key name hadeel value 
max: 23, 
min: 4,
avg: 6,
}


// lecture demo
function random(min, max) {
    return Math.floor(Math.random() * (max - min + 1) ) + min;
  }

  